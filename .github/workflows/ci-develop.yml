name: CI - Develop

on:
  push:
    branches: [develop]

permissions:
  contents: read

concurrency:
  group: auth-service-develop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/_validate.yml
